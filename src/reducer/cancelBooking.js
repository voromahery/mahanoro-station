// function bookedPlace(state = [], action) {
//     switch (action.type) {
//       case "CANCEL_BOOKING":
//         const filterArr = state.user.bookedPlace.filter((item) => item.id !== action.payload);
//         return [...filterArr];
//       default:
//         return state;
//     }
//   }
  
//   export default bookedPlace;
  